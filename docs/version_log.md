## Журнал изменений

### Версия 0.1 (начальная структура)
- создана папка `module01_environment`;
- добавлены подпапки `notebooks`, `data`, `docs`;
- помещены исходные данные `sample_text.txt` и `mini_corpus.tsv` в `data`.

### Версия 0.2 (добавление notebook)
- добавлен notebook `lr01_runtime_order_фамилия.ipynb` с результатами ЛР 1;
- проверены пути к данным в коде (относительные: `../data/...`).

### Версия 0.3 (документация)
- создан `README.md` с описанием проекта;
- заполнен `version_log.md` с историей изменений;
- проверена работа notebook после `Restart & Run All`.
